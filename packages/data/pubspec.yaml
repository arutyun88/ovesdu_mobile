name: data
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  get_it: ^7.2.0
  injectable: ^1.5.3

  domain:
    path: '../domain'

dev_dependencies:
  lints: ^2.0.0
  build_runner: ^2.2.1
  injectable_generator: ^1.5.3
